@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find required dependencies
find_dependency(PkgConfig REQUIRED)
find_dependency(Boost REQUIRED COMPONENTS container system)
find_dependency(nlohmann_json REQUIRED)

# Find optional dependencies
find_dependency(PkgConfig)
if(PkgConfig_FOUND)
    pkg_check_modules(ACL IMPORTED_TARGET libacl)
    pkg_check_modules(LIBCAP IMPORTED_TARGET libcap)
    pkg_check_modules(LIBSECCOMP IMPORTED_TARGET libseccomp)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/score-baselibsTargets.cmake")

check_required_components(score-baselibs)